# راهنمای رفع مشکلات

<div dir="rtl">

## 🔧 مشکلات رایج و راه‌حل‌ها

### 1. اپلیکیشن Hang می‌شود

**علت**: معمولاً به دلیل response null یا timeout

**راه‌حل**:
- مطمئن شوید URL صحیح است
- اتصال اینترنت خود را بررسی کنید
- timeout را افزایش دهید (پیش‌فرض: 30 ثانیه)
- سرور مقصد را بررسی کنید

### 2. خطای "No response received"

**علت**: سرور پاسخی نمی‌دهد

**راه‌حل**:
```
- بررسی URL
- بررسی اتصال شبکه
- بررسی firewall
- تست با Postman یا curl
```

### 3. خطای "Network error"

**علت**: مشکل در اتصال به سرور

**راه‌حل**:
- بررسی اتصال اینترنت
- بررسی DNS
- استفاده از VPN در صورت نیاز
- بررسی تنظیمات proxy

### 4. خطای "Request timeout"

**علت**: سرور خیلی کند پاسخ می‌دهد

**راه‌حل**:
- افزایش timeout در کد
- بررسی سرعت اینترنت
- بررسی بار سرور
- استفاده از endpoint سریع‌تر



### 5. لوگو نمایش داده نمی‌شود

**علت**: فایل‌های asset موجود نیستند

**راه‌حل**:
```yaml
# در pubspec.yaml اضافه کنید:
flutter:
  assets:
    - assets/logo.png
    - assets/logo_dark.png
```

سپس:
```bash
flutter pub get
flutter run
```

### 6. تم تیره/روشن کار نمی‌کند

**علت**: مشکل در ThemeController

**راه‌حل**:
- از Settings تم را تغییر دهید
- اپلیکیشن را restart کنید
- storage را پاک کنید

### 7. Collection ذخیره نمی‌شود

**علت**: مشکل در GetX Storage

**راه‌حل**:
```bash
# پاک کردن cache
flutter clean
flutter pub get
flutter run
```

### 8. خطای Build در Windows

**علت**: مشکل در CMake یا Visual Studio

**راه‌حل**:
```bash
# نصب مجدد dependencies
flutter clean
flutter pub get
flutter config --enable-windows-desktop
flutter run -d windows
```

## 📝 گزارش باگ

اگر مشکل شما حل نشد:

1. Issue جدید در GitHub باز کنید
2. اطلاعات زیر را ارائه دهید:
   - نسخه Flutter
   - سیستم عامل
   - پیام خطا
   - مراحل بازتولید مشکل
   - لاگ‌های مربوطه

## 🔍 دیباگ

برای دیباگ بهتر:

```bash
# اجرا در حالت verbose
flutter run -v

# مشاهده لاگ‌ها
flutter logs

# بررسی doctor
flutter doctor -v
```

## 💡 نکات مفید

- همیشه از آخرین نسخه Flutter استفاده کنید
- قبل از گزارش باگ، `flutter clean` را اجرا کنید
- لاگ‌های console را بررسی کنید
- از DevTools برای دیباگ استفاده کنید

---

**آخرین بروزرسانی**: نوامبر 2025

</div>
